<h1>New List</h1>
<%= simple_form_for([@list]) do |f| %>
  <%= f.input :name %>
  <%= f.submit "Add List", class: 'btn-submit-movie mt-3' %>
<% end %>
<div>
  <%= link_to "Back to Lists", lists_path %>
</div>
